# ─── API Backend URL ─────────────────────────────────────────────────────────
# Dev: next.config.mjs rewrites /api/* to this URL (avoids CORS in dev)
# Production (Vercel): set to https://api.techmadeeasy.info
NEXT_PUBLIC_API_URL=http://localhost:3001
